---------------kubectl commands------------------

minikube start <cluster_name>

1. display the cluster info
   kubectl cluster-info

2. display cluster nodes
   kubectl get nodes

3. dosplay all info about nodes
   kubectl describe nodes

4. to display any one node info
   kubectl describe nodes <node_name>

5. create the deployment in the cluster
   kubectl create deployment <name> --image=<image_name>

6. show the deployment
   kubectl get deployment

7. scale the deployment / create the replicas in the deployment
   kubectl scale deployment <deployment_name> --replicas=3
	 #it will create the pods/container in the cluster

8. to see the IP of pods
   kubectl get pods -o wide

9. to see the graphical interface
   minikube dashboard

10. to delete all clusters 
    minikube delete

11. to create the namespace
    kubectl create namespace <name>

12.



minikube status
 2015  kubectl create -f myrc1.yml 
 2016  kubectl get rc
 2017  kubectl get po
 2018  kubectl apply -f myrc1.yml 
 2019  kubectl get rc
 2020  kubectl get pods --show-labels
 2021  ls
 2022  kubectl 
 2023  kubectl get pods
 2024  kubectl get pods --show-labels
 2025  history | tail -50



 2002  kubectl create -f label.yml 
 2003  kubectl get po
 2004  kubectl describe pods
 2005  kubectl get pods --show-labels
 2006  kubectl get pods --selector app=web
 2007  touch myrc1.yml
  


18 march 
services
loade balancing
